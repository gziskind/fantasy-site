#! /usr/local/rvm/rubies/ruby-1.9.3-p392/bin/ruby

require 'trollop'
require 'fantasy_server'

options = Trollop::options do
	opt :port, "Server port", :default => 8080
end

port = options[:port]

FantasyServer.set :port, port
FantasyServer.start

